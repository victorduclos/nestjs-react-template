{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^14.14.25",
    "lerna": "^3.22.1",
    "typescript": "^4.1.3"
  },
  "scripts": {
    "dev": "lerna run start --parallel --stream"
  }
}
